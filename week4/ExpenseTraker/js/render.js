export const renderNavbar = (navContainerId) => {
    console.log("render navbar successfully");
};
export const renderPage = (pageName) => {
    console.log("display content inside the page succesfully");
};